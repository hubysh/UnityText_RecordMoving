using UnityEngine;
using System.Collections;
using System.Xml;
using System;

public class Replay : MonoBehaviour
{

	void Start ()
	{
		//string adr = @"C:\Users\ysh\Desktop\xmlMovements";

		//XmlDocument xmlMovements = new XmlDocument();
		//xmlMovements.Load(adr); // 주소에서 xml 파일 불러들여 저장
		//xmlMovements.

		//XmlNodeList xnList = xmlMovements.SelectNodes("/Main");

		//string po = xnList["Pos"].InnerText;
		//string ro = xnList["Rot"].InnerText;

		//Debug.Log("pos : " + po.ToString() + "rot : " + ro.ToString());

		//foreach (XmlNode xn in xnList)
		//{
		//	string pos = xn["Pos"].InnerText; // Pos 불러오기
		//	string rot = xn["Rot"].InnerText; // Rot 불러오기
		//}
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
	


//string part3 = xn["class_teacher"]["teacher"].InnerText; //선생님 이름
